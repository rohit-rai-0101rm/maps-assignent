import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Appnavigator = () => {
  return (
    <View style={styles.container}>
      <Text>Appnavigatossdr</Text>
    </View>
  );
};

export default Appnavigator;

const styles = StyleSheet.create({
  container: {},
});
